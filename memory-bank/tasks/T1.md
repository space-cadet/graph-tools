# T1: Scaffold Monorepo and Migrate Packages

## Task Info
- **ID:** T1
- **Title:** Scaffold monorepo and migrate packages
- **Status:** IN PROGRESS
- **Priority:** HIGH
- **Started:** 2026-08-10 18:12 UTC
- **Dependencies:** None

## Context

Extract `graph-core` and `graph-ui` from `spin-network-app` into a standalone monorepo `space-cadet/graph-tools`. These packages will be published as `@space-cadet/graph-core` and `@space-cadet/graph-ui`.

## Objectives

1. Create GitHub repo `space-cadet/graph-tools` ✅
2. Set up pnpm workspaces + Turborepo ✅
3. Copy graph-core from spin-network-app ✅
4. Copy graph-ui from spin-network-app ✅
5. Clean graph-core dependencies (remove UI deps) ✅
6. Fix IGraph interface (add `id` property) ✅
7. Fix cross-package imports ✅
8. Port spectral radius from econ-sim ✅
9. Set up build configuration
10. Commit and push

## Progress

1. ✅ GitHub repo created: https://github.com/space-cadet/graph-tools
2. ✅ Monorepo structure: pnpm-workspace.yaml, turbo.json, root package.json
3. ✅ graph-core copied and cleaned:
   - Removed UI deps (primereact, dexie, redux-persist, etc.)
   - Added `id` to IGraph interface
   - Added `spectralRadius()` method
   - Fixed package name to `@space-cadet/graph-core`
4. ✅ graph-ui copied and cleaned:
   - Fixed imports from `@spin-network/graph-core` to `@space-cadet/graph-core`
   - Fixed package name to `@space-cadet/graph-ui`
   - Removed `private: true`
5. ⬜ Build configuration (tsconfig, vite configs)
6. ⬜ Commit and push

## Files Created/Modified

- `/packages/graph-core/package.json` — Cleaned deps, updated name
- `/packages/graph-core/src/core/types.ts` — Added `id` to IGraph
- `/packages/graph-core/src/core/GraphologyAdapter.ts` — Added `id`, `spectralRadius()`
- `/packages/graph-ui/package.json` — Cleaned deps, updated name
- `/packages/graph-ui/src/**/*.ts` — Fixed imports

## Next Steps

1. Verify tsconfig files are correct
2. Test build with `pnpm build`
3. Commit and push to GitHub
